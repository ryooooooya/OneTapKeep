# NextAuth
# ローカル開発時のみ必要。Vercelでは自動設定される
NEXTAUTH_URL=http://localhost:3000
# openssl rand -base64 32 で生成。Vercelの環境変数にも設定が必要
AUTH_SECRET=

# Google OAuth
# Google Cloud Console > APIとサービス > 認証情報 から取得
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Upstash Redis (Vercel Marketplace から追加)
# Vercel にIntegrationを追加すると自動設定される
KV_REST_API_URL=
KV_REST_API_TOKEN=
